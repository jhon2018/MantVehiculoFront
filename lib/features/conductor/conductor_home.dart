import 'package:flutter/material.dart';

class ConductorHome extends StatelessWidget {
  const ConductorHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Bienvenido Conductor')),
    );
  }
}
